background {
	monitor =
path = /home/jeremy/Pictures/wallpapers/mask.png

	blur_passes = 1 # 0 disables blurring
    blur_size = 7
    noise = 0.0117
    contrast = 0.8916
    brightness = 0.1
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

label {
    monitor =
text = He is the MELBA-BEING ... the ANGEL CAKE ... XEROX him ... XEROX him --
    text_align = center # center/right or any value for default left. multi-line text alignment inside label container
    color = rgba(c0caf5)
    font_size = 25
    font_family = Noto Sans
    rotate = 0 # degrees, counter-clockwise

    position = 0, 80
    halign = center
    valign = center
}

input-field {
    monitor =
    size = 250, 50
    outline_thickness = 3
    dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
    outer_color = rgb(7aa2f7)
    inner_color = rgb(1a1b26)
    font_color = rgb(c0caf5)
    fade_on_empty = true
    fade_timeout = 1000 # Milliseconds before fade_on_empty is triggered.
text = He is the MELBA-BEING ... the ANGEL CAKE ... XEROX him ... XEROX him --
    hide_input = false
    rounding = -1 # -1 means complete rounding (circle/oval)
    check_color = rgb(9ece6a)
    fail_color = rgb(f7768e) # if authentication failed, changes outer_color and fail message color
text = He is the MELBA-BEING ... the ANGEL CAKE ... XEROX him ... XEROX him --
    fail_timeout = 2000 # milliseconds before fail_text and fail_color disappears
    fail_transition = 300 # transition time in ms between normal outer_color and fail_color
    capslock_color = rgb(9d7cd8)
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false # change color if numlock is off
    swap_font_color = false # see below

    position = 0, -20
    halign = center
    valign = center
}
