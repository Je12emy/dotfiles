# Remove the defailt C-b prefix bind
unbind C-b
# Bind the prefix key to C-a
set -g prefix C-a

# Vim like key binds for moving between panes
bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -R
