- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.config/:
      path: config/*
      glob: true
    ~/.tmux.conf: .tmux.conf
    ~/.bashrc: .bashrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
