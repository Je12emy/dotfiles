- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config', '~/.local/bin']

- link:
    ~/.config/: 
      path: config/*
      glob: true
    ~/.local/bin:
      path: scripts/*.sh
      glob: true
    ~/.zshenv: .zshenv

- create:
  - ~/.config/scripts

- shell:
  - [git submodule update --init --recursive, Installing submodules]
